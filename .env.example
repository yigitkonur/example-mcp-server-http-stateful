# HTTP runtime
PORT=1453
HOST=127.0.0.1
CORS_ORIGIN=*

# Session lifecycle
SESSION_TTL_MS=1800000
CLEANUP_INTERVAL_MS=60000

# Event store (SSE resumability)
EVENT_MAX_AGE_MS=86400000
EVENT_MAX_COUNT=5000

# Optional: comma-separated hostnames for Host header validation
# ALLOWED_HOSTS=localhost,127.0.0.1,[::1]
